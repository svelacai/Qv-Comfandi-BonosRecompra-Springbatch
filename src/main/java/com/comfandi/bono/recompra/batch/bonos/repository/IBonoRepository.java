package com.comfandi.bono.recompra.batch.bonos.repository;

import java.time.LocalDate;
import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.repository.query.Param;

import com.comfandi.bono.recompra.batch.bonos.dto.BonosDTO;
/*
public interface IBonoRepository extends JpaRepository<BonosDTO, Long> {
	public List<Object[]> findBonosDTO(@Param("campaignId") String campaignId, @Param("plu") String plu, @Param("endDate") LocalDate endDate);

}
*/